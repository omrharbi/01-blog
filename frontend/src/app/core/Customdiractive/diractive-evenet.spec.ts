import { DiractiveEvenet } from './diractive-evenet';

describe('DiractiveEvenet', () => {
  it('should create an instance', () => {
    const directive = new DiractiveEvenet();
    expect(directive).toBeTruthy();
  });
});
